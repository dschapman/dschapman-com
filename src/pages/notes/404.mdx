import Layout from '../../components/layout/layout'
export default (props, location) => (
  <Layout
    title="❓ Note not found..."
    description="Something went wrong."
    type="📝"
    location={location}
    {...props}
  />
)

The note you were looking for has not been published yet.
